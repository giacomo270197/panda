declare("MessageBoxA", "user32.dll");

import libraries.utils.strings

int fn main() {
  string a = "Hello ";
  string b = "world!";
  string c = "Caption";
  MessageBoxA(0, strcat(a, b), c, 0);
  return 1;
}